
  ## 열거형 (enum)
 관련된 상수들을 묶어 놓은 클래스로 상수의 값과 타입을 체크 해주기 때문에 타입 안정성을 높일 수 있다.
 (논리적 에러를 컴파일 에러로 확인할 수 있음.)
 - 열거형 상수의 값은 기본값이 아닌 객체.
 - 열거형 상수의 비교에는 ==연산자 과 compareTo()를 사용 가능하지만 비교연산자(>, <)는 불가능하다.
 - 모든 열거형은 Enum class의 자식 클래스이며 enum method를 상속받는다.
     ( Enum Method )
        name() : 열거형의 상수 이름을 문자열로 반환
        ordinal() : 열거형 상수가 정의된 순서를 반환(0부터 시작)
        valueOf(enumType, String name) :  지정된 열거형에서 name과 일치하는 열거형 상수를 반환.
 ex1 | 열거형의 메서드를 사용해보자.

 - 만약 불연속적인 열거형 상수일 경우, 정의하는 열거형 상수 이름에 원하는 값을 괄호 추가하여 작성한다.
 - 괄호()를 사용하려면 인스턴스 변수와 생성자를 새로 추가 해주어야 한다.
    (추가된 값이 저장될 인스턴스 변수와 헤당 값을 받을 수 있는 생성자.)
 - 열거형의 생성자는 묵시적으로 private이므로, 외부에서 호출 할 수 없다. (=> 외부에서 별도로 객체 생성 불가 )
 ex2 | 불연속적인 열거형을 정의해보자.