
## (메서드) 오버라이딩 (overriding)

- 자식 클래스가 상속받은 부모의 메서드를 자신에 맞게 변경하는 것 [ 덮어쓰다. ]
- 선언 부는 변경할 수 없고 구현 부만 변경 가능하다.
- 오버라이딩하는 메서드의 접근제어 자는 부모 클래스의 메서드보다 좁은 범위로 변경 불가능하다.
- 예외는 조상 클래스의 메서드보다 많이 선언할 수 없다.

- Ex1 | 자식 클래스는 상속받은 부모 클래스의 메서드를 호출하여 구현부를 변경하여 사용할 수 있다.
- Ex2 | Object가 모든 클래스를 기본적으로 상속한다는 것을 이용하여 클래스의 변수 값을 출력하는 과정을 줄일 수 있다.
- Ex3 | 오버로딩과 오버라이딩을 구분할 수 있다.

  오버로딩 (overloading) | 상속과 관계 X |
- 메서드의 이름이 같지만 매개변수를 달리하여 기존에 없는 새로운 이름의 메서드를 정의하는 것

  오버라이딩(overriding) | 상속과 관계 O |
- 상속 받은 부모의 메서드의 구현부만 달리하여 기존에 있는 메서드를 덮어 쓰듯 변경하는 것


