
## 매개변수 (Parameter)
- 메서드의 입력값을 받는 변수이다.

## 기본형 매개변수(Primitive Parameter)
- 변수의 값을 복사하기 때문에 값을 읽기만 할 수 있는 매개변수 타입이다.
- ex2 | 기본형 매개변수를 활용한 메서드의 호출 스택 변화를 알 수 있다.

## 참조형 매개변수(Reference Parameter)
- 인스턴스의 주소가 복사되어 값을 읽고 변경할 수 있는 매개변수 타입이다.
- ex3 | 참조형 매개변수를 활용한 메서드의 호출 스택 변화를 알 수 있다.

##참조형 반환타입
- 반환하는 값의 타입이 참조형이고 모든 참조형 타입의 값은 '객체의 주소'이므로 그저 정수값이 반환되는 것이다.
- ex4 | 참조형 반환타입을 활용한 메서드의 호출 스택 변화를 알 수 있다.
