
 ## Object 클래스

- 모든 클래스의 부모 클래스.
- 11개의 메서드를 가지고 있고, 클래스는 이 메서드들을 오버라이딩하여 사용한다.

 Object Method
 clone()
- 객체 자신의 복사본을 반환하는 메서드로 protected 접근 제어자를 같이 사용한다
=> 해당 메서드를 사용하는 클래스는 오버라이딩으로 접근제어자를 public로 바꿔야 편하다.

 equals(Object o)
- 매개변수로 객체의 참조변수를 받아서 비교하여 그 결과를 boolean값으로 알려주는데
  객체는 한 주소에 하나의 객체를 만들고 equals()는 참조변수를 받아서 비교하기 때문에 객체 주솟값이 다르다면 결과는 flase가 나온다.
-ex1 | equals()를 사용할 수 있다.
-ex2 | int타입 변수에 equals()를 사용할 수 있다.
-ex3 | String타입 변수에 equals()를 사용할 수 있다.

 getClass()
- 객체의 클래스 정보를 담고 있는 Class인스턴스를 반환한다.

 hashCode()
- 객체 자신의 hashCode를 반환한다.

 toString()
- 객체 자신의 정보를 문자열로 반환한다.
 notify(), notifyAll(), wait(), wait( long timeout ), wait( long timeout, int nanos )
- 쓰레드에서 자주 사용하는 메서드

